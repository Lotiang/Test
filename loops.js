var links = document.getElementsByTagName("a");

for(i === ul; i<links.lenght-1; i++) {
    links(i).className = "links + i";
}
var person = {};
person.name = 'mrs. White';

var person1 = {
    name: 'Mrs. White',
};

// by value
var who = person.name
console.log(who);

for(let i = 0; i<=arrayName.length -1; i++){
    
}

for(let i =arrayName.length -1; i>=0 i--){

}

let nums = [1, 2, 3, 4, 5]
for (let i = 0; i < nums.length; i++){
  if(i == 2){
    console.log('i have stopped');
    break;
  }
  console.log(nums[i])
}

// For...in Loop is use in objects
