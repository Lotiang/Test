// JavaScript Statements
// The const Statement
// Declaring a constant object does NOT make the properties unchangeable:

// Car owner is Johnson


// Create an object:
const car = {type:"Fiat", model:"500", color:"white"};

// Change a property:
car.color = "red";

// Add a property:
car.owner = "Johnson";