// example of an object syntax
let studentInfo = {
    name: 'Lotiang',
    age: 28,
    isSouthSudanese: true,
    marks: [37,90,90],
}
console.log(studentInfo.age)

// ++i increment increases the value of i by 1 and it uses the new statements
// i++ increament increases the value of i by 1 and it uses the original value
// index++ = 1 post increment
// ++index = -1 post decrement
// arrays
// arrays consist of the following
// arrays of strings
// arrays of numbers
// arrays of booleans
// array => let student = []
let name = 'Lotiang'
// split each character
let nameArray = name.split('');
// access A
console.log(name.charAt(0));
console.log(nameArray[0]);

// Names array
let names = ['Lotiang', 'Nyathio', 'Lokiru']
console.log(names[0])
console.log(names[1])
console.log(names[2])

// fullname
console.log(names[0]+''+names[1]+''+names[2])

let num1 = 2
console.log(num++);
var num2 = num++
console.log(num2++);
let num3 = 2
console.log(++num3);

// way to joins arrays
array.joins
name.concat
console.log(names[0]+''+names[1]+''+names[2])

// for loop
for(let index = 0, index<array.length; index++) {

}
let num = 1
let num2 = num++ + 2
console.log(num, num2);